# Copy to .env and fill in — this file is safe to commit.
# .env itself is gitignored.

# Postgres connection string (same as backend/.env)
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/budjet

# Optional: override DuckDB file location
# DUCKDB_PATH=data_platform/budjet.duckdb
